# Telemetry — Workflows
**Status:** Stub (Sprint 1)

Workflow-level metrics will be written here as JSON records once
workflow telemetry runtime is active.

See: `workflow_metrics_template.json` for the target schema.
